awk数据过滤,统计软件(grep)
awk 选项 '条件{i++}' 文件 -F

1./正则/, $3~/正则/ $3~/root/
2.字符和数字  == != > >= < <=
3. && 并且 || 或者

awk 选项  '条件{指令}' 文件
awk 选项 '{}'  文件

if(判断条件){
  命令
}
if(条件){
  命令
}
else {}

echo -e "\033[15;30Hx"

统计日志中每个IP访问的次数
awk '{IP[$1]++}'
ab -c 100 -n 100 http://172.25.0.11/
模拟生产环境访问http
